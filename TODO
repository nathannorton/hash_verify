* add pid file and locking so 2 runs can not overlap
* Does not differentiate between text and binary files yet. 
* make authentication work
* need to do exception checking on all file system calls
* make sure adding hash will check if has not exists and also not changed if already there 
